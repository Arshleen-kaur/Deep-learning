Problem with Perceptron
1. It does not work with XOR data
2. It does not work wirth non-linear data

MLP : Multi-Layer Perceptron
Trained with Backprop
Hidden layers which have multiple perceptrons which capture linearity but a combination of them captures non-linearity
Nodes can be added in hidden layersanother way is to increase input layer nodes. (from 2d to 3d)
combining two planes linearly
Another way is increasing nodes in output layer. (multi-class classification)
Another way is increasing the number of hidden layers
Neural networks are UFA 

 n=4 (4D data)
 How many trainable parameters
 weights                        | biases
 (input layer* hidden layer 1)  | (hidden layer 1 biases)
 (hidden layer 1* hidden layer2)| (hidden layer 2 biases)
 (hidden layer 2 * output layer)| (1 bias output layer)